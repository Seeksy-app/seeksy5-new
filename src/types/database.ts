// Re-export the auto-generated Supabase types as the canonical Database type
// This ensures all components use the same complete schema
export type { Database, Json } from '@/integrations/supabase/types';
